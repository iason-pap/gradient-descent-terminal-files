List directory:
ls

Change directory:
cd <directory-name>
PS: "cd .." navigates to previous layer

Import Decryption Key:
gpg --import <key-name>.gpg

Decrypt secured documents:
gpg -d <document-name>

Remove french language package:
rm -fr .
